check::
	kubectl get pods -n ${namespace}

deploy::
	@${kustomize} | ${kubectl} apply -f -

run:: deploy test

stop::
	${kustomize} | ${kubectl} delete --selector delete!=persist -f - --ignore-not-found
